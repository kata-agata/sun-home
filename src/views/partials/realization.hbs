{{>main/header}}
<section id="realization" class="pt-5">
<div class="d-flex flex-row w-75 mx-auto align-items-center">
  <div class="col-6 text-center">
    <p class="h3">{{realization.title}}</p>
    <p>{{{realization.sanitizedHtml}}}</p>
  </div>
  <div class="col-4">
    <img class="img-fluid" src="{{realization.topImage}}" alt="">
  </div>
</div>
<div class="d-flex flex-row w-75 mx-auto">
  <div class="card-columns">
    {{#each realization.images}}
      <a href="#" data-toggle="modal" data-target="#realization-gallery-modal" data-imgsrc="\\{{this}}" data-slide-to="{{@index}}">
        <div class="card">
          <img src="\\{{this}}" class="card-img-top" alt="...">
        </div>
      </a>
        {{else}}
        <p>No images</p>
    {{/each}}
  </div>
</div>

  <div class="modal fade" id="realization-gallery-modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="figure">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>

        <div class="modal-body">

          <div id="carouselExample" class="carousel slide" data-ride="carousel">
          <div class="carousel-inner">
            {{#each realization.images}}
            <div class="carousel-item">
              <img class="d-block w-100" src="\\{{this}}">
            </div>
            {{/each}}
          </div>
          <a class="carousel-control-prev" href="#carouselExample" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
          </a>
          <a class="carousel-control-next" href="#carouselExample" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
          </a>
        </div>
      </div>


        </div>
      <!-- <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button>
      </div> -->
    </div>
  </div>
  </div>
</section>
